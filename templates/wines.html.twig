{# templates/shop.html.twig #}
{% extends 'base.html.twig' %}
{% block content %}
    <div class="site-section mt-5">

    <div class="container">
        <div class="row categories">
            {% for item in categories %}
                <div class="col-lg-4 mb-5 col-md-6">
                    <div class="wine_v_1 text-center pb-4">
                        <a href="{{ path('product_page', { '_locale' : 'fr', 'categorie' : item.id}) }}" class="thumbnail d-block mb-4"><img src="{{ asset(item.visuel) }}" alt="Image" class="img-categorie"></a>
                        <div>
                            <h3 class="heading mb-1"><a href="#">{{ item.libelle }}</a></h3>
                        </div>


                        <div class="wine-actions">
                            <h3 class="heading-2">Découvrir les produits<a href="#"></a></h3>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    </div>
{% endblock %}