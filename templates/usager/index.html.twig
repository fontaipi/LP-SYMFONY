{% extends 'base.html.twig' %}

{% block title %}Usager index{% endblock %}

{% block content %}
    <div class="site-section mt-5">

        <div class="container">
            <div class="row">
                <div class="col-lg-12 mb-12 col-md-12">
                    <h2>Mon compte</h2>
                    <div class="user">
                        Bienvenue {{ usager.email }}
                    </div>
                </div>
                <div class="col-lg-6 mb-6 col-md-6">
                    <a class="btn btn-primary btn-lg btn-block" href="{{ path("panier_index") }}">Mon panier</a>
                </div>
                <div class="col-lg-6 mb-6 col-md-6">
                    <a class="btn btn-primary btn-lg btn-block" href="{{ path('app_logout') }}">Déconnexion</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
